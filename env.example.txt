# Public Next.js routing values. Both URLs must identify the same deployment.
NEXT_PUBLIC_CONVEX_URL=https://your-deployment.convex.cloud
NEXT_PUBLIC_CONVEX_SITE_URL=https://your-deployment.convex.site
NEXT_PUBLIC_SITE_URL=http://localhost:3000

# Configure these as Convex deployment environment variables; never expose them to the browser.
SITE_URL=http://localhost:3000
BETTER_AUTH_SECRET=generate-at-least-32-random-bytes
INVITE_TOKEN_SECRET=generate-a-separate-at-least-32-byte-secret
INITIAL_ADMIN_EMAIL=the-initial-administrator@example.com

NEXT_PUBLIC_SENTRY_DISABLED=true
